# Supabase — Project Settings > API
# Copie para .env.local e preencha. Sem estas variáveis o app roda
# em modo local (localStorage), sem base de dados.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
